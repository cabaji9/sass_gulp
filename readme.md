##BASIC GULP WITH SASS

Made this little project to learn sass and gulp
just need to :

run
```
$ npm install
```
then

install gulp on global level
```
npm install -g gulp
```
##RUNNING

Just run it with:

$ gulp
